# Copy this file to `.env.local` and fill in your real Anthropic key for local dev.
# NEVER commit `.env.local` — it's in .gitignore for a reason.
# In production, set this in Vercel's dashboard instead (Settings → Environment Variables).

ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
